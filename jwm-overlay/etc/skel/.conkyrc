conky.config = {
own_window = true,
own_window_class = 'Conky',
own_window_type = 'desktop',
own_window_transparent = true,
own_window_hints='below',
out_to_console = true,
update_interval = 1.0,
total_run_times = 0,
out_to_x = yes,
double_buffer = true,
use_xft = true,
font = 'Cantarell:size=10',
use_spacer = 'none',
alignment = 'top_middle',
gap_x = 5,
gap_y = 20,
default_color = '191919',
}
conky.text = [[
${time %a %e, %b %Y}, Brasília (BR) ${weather http://weather.noaa.gov/pub/data/observations/metar/stations/ SBBR temperature temperature 15}°C | Uptime: ${uptime_short} | Root: ${fs_used_perc /}% (${fs_free /} - ${fs_used /}) | CPU: ${cpu cpuN}% | RAM: $memperc% ($mem) | Battery: ${battery BAT1} |${if_existing /proc/net/route wlp3s0} Connection: wiriless ${wireless_essid} ${wireless_link_qual wlp3s0}% ${else} ${if_existing /proc/net/route enp6s0}Connection: cable ${else}Connection: no available ${endif}${endif} | Updates: ${execpi 10800 checkupdates | wc -l}
]]